# stdio-транспорт MCP

Проработка направления. Карточка в общей стратегии: [`../../STRATEGY.md`](../../STRATEGY.md) §3.2.

**Статус:** идея (детализация позже).

## Открытые вопросы

- Один артефакт `installDist` — два entrypoint (HTTP / stdio) или флаги.
- Документация для Cursor/агентов: рядом с Docker HTTP.
- Телеметрия: как вести метрики при stdio ([`../mcp-telemetry-grafana/`](../mcp-telemetry-grafana/)).
